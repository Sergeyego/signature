import { PdfView } from "../../internal/buffer/view";

export class RawData {
  public static EMPTY_VIEW = new PdfView();

  public raw: PdfView = RawData.EMPTY_VIEW;

  public isEmptyRaw(): boolean {
    return !this.raw.length;
  }
}
